allow vold proc_touchpanel:dir r_dir_perms;
allow vold system_block_device:blk_file getattr;
allow vold logd:dir { getattr open read };
allow vold persist_file:dir { open read };

allow vold proc_fingerprint:dir { read open };

allow vold system_block_device:blk_file r_file_perms;
#allow vold block_device:blk_file { open read write getattr ioctl };

allow vold persist_file:dir ioctl;

